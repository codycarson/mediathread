# application config
app_name: mediathread
backups_dir: /opt/backups
git_repo_branch: appsembler_develop
git_repo_dir: /opt/mediathread
venv_dir: /opt/venv/

# memcached config
memcached_memory: 32

# django static files
cloudfront_url: "da6w4csgk5z80.cloudfront.net" # without the http(s) part and the trailing slash
django_media_dir: "{{git_repo_dir}}/uploads"
django_project_dir: "{{git_repo_dir}}/mediathread"
django_static_dir: "{{git_repo_dir}}/collected_static"

#backups
s3_backup_bucket: mediathreadbackups
