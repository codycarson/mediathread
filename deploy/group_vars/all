# application config
app_name: mediathread
git_repo_branch: appsembler_master
git_repo_dir: /opt/mediathread
server_name: mediathread.appsembler.com
venv_dir: /opt/venv/

# memcached config
memcached_memory: 32

# django static files
cloudfront_url: "d1s9voh9zis26j.cloudfront.net" # without the http(s) part and the trailing slash
django_media_dir: "{{git_repo_dir}}/media"
django_project_dir: "{{git_repo_dir}}/mediathread"
django_static_dir: "{{git_repo_dir}}/collected_static"
